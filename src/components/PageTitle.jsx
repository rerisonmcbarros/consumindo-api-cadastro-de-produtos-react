const PageTitle = ({children}) => {
  return (
    <h1 className="border rounded-md bg-blue-500 text-slate-50 p-2 ps-5 mb-8 shadow-md text-xl">
      {children}
    </h1>
  )
}

export default PageTitle
